# Local development — copy to .env.local (or run: npm run env:local)
APP_ENV=local

DJANGO_DEBUG=1
# Optional: any non-empty value works locally. Placeholders fall back to a local-only key.
DJANGO_SECRET_KEY=dev-only-change-me
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,testserver
# Uncomment only when sharing the API on your LAN (Host header / CORS open):
# DJANGO_ALLOW_ALL_HOSTS=1
# DJANGO_CORS_ALLOW_ALL=1
FRONTEND_URL=http://localhost:5175

# Local MySQL (XAMPP / WAMP / Docker / native)
DB_NAME=holidays_app
DB_USER=root
DB_PASSWORD=root
DB_HOST=localhost
DB_PORT=3306

# Emails print in terminal during dev (no SMTP below).
# To send real emails locally, uncomment and fill SMTP settings:
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=1
# EMAIL_HOST_USER=your@gmail.com
# EMAIL_HOST_PASSWORD=your-app-password
# DEFAULT_FROM_EMAIL=Gestion de congé <your@gmail.com>
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
